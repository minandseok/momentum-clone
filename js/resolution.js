const resolution = document.querySelector(".resolution");

const resolutionList = [];

// const selectedResolution = quotes[Math.floor(Math.random() * quotes.length)];

// resolution.innerText = selectedResolution;
